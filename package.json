{
  "name": "travel-around",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:smoke": "playwright test",
    "check:mobile": "node scripts/mobile-visual-check.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
