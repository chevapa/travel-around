The album view: every frame as a cell on a darkroom contact sheet.

```jsx
<ContactSheet frames={frames} range="2023—2026" onPick={openFrame} />
```

Always on the ink surface with thin paper hairlines, so it reads as a photographic sheet rather than a gallery grid. Unprinted frames stay in the grid as dashed blanks — that absence is the point.
