A small uppercase mono tag for a frame's categories.

```jsx
<Tag>Croatia</Tag>
<Tag tone="ink">52 min · 49 km</Tag>
```

`outline` for categories, `ink` for hard data (drive time, distance), `yellow` for the sort key in a panel footer. Tags sit at the bottom of a card — the place name always comes first.
