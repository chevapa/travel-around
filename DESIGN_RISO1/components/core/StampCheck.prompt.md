The system's checkbox: an ink square stamped with a hand-drawn pink ✕.

```jsx
<StampCheck checked={on} onChange={setOn} />
```

Used only in The Index. Critical rule: an unchecked row is shown by the empty box, never by greying its label — every filter label stays full-ink at 16px.
