Drive-time rings and their stamped yellow labels — the way trips are actually chosen in this product.

```jsx
<RingSet />
<RingLabel tilt={-4}>2 H</RingLabel>
```

Rings are 3px dashed ink at low alpha so they sit under the prints; labels are yellow pennants with a small tilt. Keep labels on the ring's left edge where the map is emptiest.
