The Atlas's permanent key, bottom-left, on ink — and the filter people actually use.

```jsx
<Legend counts={{ loved: 31, fine: 11, unprinted: 74 }} active={iso} onToggle={setIso} />
```

Always visible: the map's three states are encoded in colour, and nothing else on screen explains them. Tapping a row isolates that state. Keep it on the ink surface — over torn-photo terrain, paper would lose contrast.
