A map cluster drawn as a stack of prints with the count scrawled on the top sheet.

```jsx
<FrameStack count={4} onClick={zoomIn} />
```

Rule inherited from the critique: clusters stay quiet (paper + ink outline, handwritten numeral) while individual Prints keep the colour and weight. A cluster that shouts louder than a place inverts the hierarchy.
